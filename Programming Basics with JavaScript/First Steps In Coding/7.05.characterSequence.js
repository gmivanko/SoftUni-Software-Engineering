function characterSequence(input) {
    let text = input[0];

    for (let i = 0; i < text.length; i++) {
        // let letter = text[i]
        // console.log(letter);
        console.log(text[i]);
    }

    // let text = input[0];
    // let buff = ""
    // for (let i = 0; i < text.length; i++) {
    //     buff += text[i] + " "
    // } console.log(buff);
}

characterSequence(["softuni"]) 